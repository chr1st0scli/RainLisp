namespace RainLisp
{
    public enum TokenType
    {
        Number,
        String,
        Boolean,
        Identifier,
        LParen,
        RParen,
        Quote,
        Assignment,
        Definition,
        If,
        Begin,
        Lambda,
        EOF
    }
}
